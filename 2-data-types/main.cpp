#include <iostream>


int main(int argc, char const *argv[]) {
    /**
     *  's'
     */
    std::string test = "Luca";
    std::cout << test << std::endl;

    unsigned int b = 1; // unsigned can store double the values of a traditional int.
    // It does not use the first bit to identify the sign
    return 0;
}